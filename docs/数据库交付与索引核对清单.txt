理发预约系统 数据库交付与索引清单

A. 交付文件清单（必须）
1) uniCloud-aliyun/database/*.schema.json
2) uniCloud-aliyun/database/*.index.json
3) uniCloud-aliyun/database/seed-*.jql
4) demo-data/*.example.json（脱敏示例数据）

说明：公开仓库不交付真实数据库导出，不包含验证码、登录 token、真实手机号、真实订单或审计日志。

B. 核心表核对（14张）
1) aftersales
2) audit_logs
3) auth_tokens
4) barber_schedules
5) notifications
6) order_events
7) order_items
8) orders
9) reviews
10) services
11) sms_codes
12) stores
13) time_slots
14) users

C. 13个核心业务索引
说明：以下索引以 `uniCloud-aliyun/database/*.index.json` 为准（与控制台名称一致）。

1) orders.idx_orders_user_updated
   字段：userId(升序) + updatedAt(降序)
   用途：用户订单列表、分页与修改时间排序

2) orders.idx_orders_store_date_updated
   字段：storeId(升序) + date(升序) + updatedAt(降序)
   用途：门店订单列表、按日期查询、增量同步

3) orders.idx_orders_barber_date_status
   字段：barberId(升序) + date(升序) + status(升序)
   用途：理发师当日工单、待服务队列

4) orders.idx_orders_verify_code
   字段：verifyCode(升序)
   用途：到店核验码快速查单

5) order_events.idx_order_events_order_ts
   字段：orderId(升序) + ts(降序)
   用途：订单时间线/操作日志按时间倒序查询

6) reviews.idx_reviews_store_created
   字段：storeId(升序) + createdAt(降序)
   用途：门店评价列表、最新评价

7) notifications.idx_noti_user_read_created
   字段：userId(升序) + isRead(升序) + createdAt(降序)
   用途：用户通知列表、未读筛选、按时间倒序

8) aftersales.idx_aftersales_store_status_updated
   字段：storeId(升序) + status(升序) + updatedAt(降序)
   用途：门店售后工单处理列表

9) aftersales.idx_aftersales_user_created
   字段：userId(升序) + createdAt(降序)
   用途：用户“我的售后”分页与倒序查询

10) time_slots.idx_slots_barber_date_start（唯一）
   字段：barberId(升序) + date(升序) + startTime(升序)
   用途：时段唯一约束，防止重复写入同一时段

11) barber_schedules.idx_sched_barber_date
   字段：barberId(升序) + date(升序)
   用途：理发师排班查询与编辑回显

12) services.idx_services_store
    字段：storeId(升序)
    用途：门店服务项列表查询

13) users.idx_users_username（唯一）
    字段：username(升序)
    用途：登录账号唯一约束

D. 索引文件清单
1) orders.index.json（4条）
2) order_events.index.json
3) reviews.index.json
4) notifications.index.json
5) aftersales.index.json
6) time_slots.index.json
7) barber_schedules.index.json
8) services.index.json
9) users.index.json

E. 检查项
[ ] schema 已上传
[ ] index.json 已同步到云端
[ ] init 数据已导入
[ ] 13个核心索引存在
[ ] 登录/下单/到店/开服务/完服务链路可跑通
