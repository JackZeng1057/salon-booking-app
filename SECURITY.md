# Security Policy

如果你发现安全问题，请不要直接公开 Issue 或在评论区贴出漏洞细节。请优先通过 Gitee 或 GitHub 仓库主页联系维护者，并尽量提供可复现步骤、影响范围和建议修复方式。

本项目为本科毕业设计项目，不建议直接用于生产环境。生产环境使用前，请自行完成密钥管理、权限隔离、接口限流、日志审计、隐私合规、备份恢复、异常监控和安全测试。

请勿提交以下内容：

- `config.json`、`.env`、`*.local` 等本地真实配置。
- API Key、AccessKey、AppCode、短信签名或模板密钥。
- 登录 token、短信验证码、真实手机号、真实订单、售后描述、审计日志。
- APK、签名证书、构建缓存或其他二进制构建产物。

允许提交的公开样例材料包括：

- `config.example.json` 这类占位配置。
- `demo-data/*.example.json` 这类脱敏样例。
- `static/app-icons/*.png` 这类打包所需的非敏感静态资源。
